{
  "name": "@pinloom/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "Electron shell — a standalone window onto the locally-running pinloom (escapes the browser tab).",
  "main": "main.cjs",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^33.4.11"
  }
}
