node_modules
dist
*.tsbuildinfo
.DS_Store
.env
.env.local
*.log
data/*.sqlite
data/*.sqlite-*
data/*.deleted-backup
# SQLite DBs also land under a package's own data/ dir when the backend runs
# with a cwd-relative PINLOOM_DB_PATH (e.g. `dev:terminal` → packages/backend/data/).
**/data/*.sqlite
**/data/*.sqlite-*
.vite

# Playwright
test-results
playwright-report
playwright/.cache

# Claude Code local state
.claude/

# Playwright MCP transient snapshots / e2e screenshots
.playwright-mcp/
e2e-*.png
e2e/artifacts/

# oh-my-claudecode local project state
.omc/

# Personal task tracker — kept out of the public repo. Per-developer
# convention is to store notes under `tasks/done/<YYYY-MM-DD>/`; the
# folder lives in working trees but never reaches GitHub.
tasks/
